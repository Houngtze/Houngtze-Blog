
$accentColor = #C2A7C2 //默认主题颜色
$textColor = #22222A //默认字体颜色
$borderColor = #D0D3DA //默认边框颜色

//示例修改相关样式f12找到需要修改的地方找到对应class类拿过来直接用就行了
.sidebar-group.is-sub-group > .sidebar-heading:not(.clickable) {
  opacity: 1
}
